{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm --prefix client ci && npm --prefix client run build",
  "functions": {
    "index.py": {
      "maxDuration": 60,
      "includeFiles": "{backend/**,forntend/**,client/dist/**}"
    }
  }
}
